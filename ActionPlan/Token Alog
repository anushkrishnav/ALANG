start
Declare INTEGER ,PLUS,EOF as string
Get and set type and value as input 
Now the flow moves to __repr__()

Now the Flow conrol has been shifted to __repr__()
    __str__() is called

Now the Flow conrol has been shifted to __str__()
    //str class returns string representation of object
    Returns string 'Token({Type},{Value})' where value is converted into string

Now the flow control has been shifted to __repr__()
    returns the string generated by  __str__()

Exit